<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-950">
        <h1 class="text-white px-5 py-3 text-3xl">Task Builder</h1>
        <div class="form p-10 text-white">
            <form action="/create" method="post">
                <input class="w-full rounded-md outline-none block px-5 py-2 bg-zinc-600" type="text" placeholder="Title goes here..." name="title">
                <textarea class="w-full rounded-md outline-none block px-5 py-2 resize-none bg-zinc-600 mt-2" placeholder="Write your task details here... " name="details"></textarea>
                <input class="mt-2 bg-blue-600 px-5 py-2 rounded-md" type="submit" value="Create Task">
            </form>
        </div>
        <div class="tasks p-10 flex flex-wrap gap-3">
        <% if(files.length>0){%> 
            <% files.forEach(function(val){ %> 
            <div class="task w-72 px-3 py-4 rounded-md bg-zinc-700 ">
                <h1 class="text-white text-2xl tracking-tighter"><%=val%></h1>
                <div class="flex w-full justify-between items-center mt-3">
                    <a href="/file/<%= val%> " class="text-blue-500 inline-block">Read More</a>
                    <a class="text-amber-600 ml-10" href="/edit/<%= val %>">Edit file name</a>
                </div>
            </div>
            <% }) %>
        <%}else{%>
            <h3 class="text-zinc-300">No tasks yet.</h3>
        <%}%>
        </div>
    </div>
</body>
</html>